const fs = require('fs'); const data = fs.readFileSync('data.js', 'utf8'); const json = fs.readFileSync('../notebooklm_recipes.json', 'utf8'); const start = data.indexOf('const RECETAS_NUEVAS = {'); const end = data.indexOf('function getSemanaActual() {'); const newData = data.substring(0, start) + 'const RECETAS_NUEVAS = ' + json + ';\n\n// Función para obtener el menú de la semana actual\n' + data.substring(end); fs.writeFileSync('data.js', newData); console.log('Merged successfully!');
